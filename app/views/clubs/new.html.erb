<h2>New Club</h2>

<%= form_for(@club) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= fields_for(@deposit) do |d| %>
    <%= d.label :amount %>
    <%= d.number_field :amount %>
  <% end %>
  <%= f.submit %>
<% end %>